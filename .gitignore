.DS_Store
build/
.superpowers/
*.xcodeproj

# Never commit OAuth client credentials
client_secret*.json
oauth-client*.json
!Config/oauth-client.example.json
*.credentials.json

# Generated from Config/oauth-client.json by
# scripts/generate-oauth-credentials.sh — never committed, since a real
# build bakes the actual client secret into it. See README.md.
Sources/RavenFeature/Generated/BakedOAuthCredentials.swift

# Belt-and-braces: raw (unredacted) Gmail fixture captures live at
# ~/.config/ainkrad-raven/raw-fixtures/, outside this repo, and must never be
# committed even if something goes wrong and a copy ends up inside it.
raw-fixtures/

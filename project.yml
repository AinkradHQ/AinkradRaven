name: AinkradMail
options:
  bundleIdPrefix: com.ainkrad
  deploymentTarget: { macOS: "14.0" }
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGNING_REQUIRED: NO
    CODE_SIGNING_ALLOWED: NO
    EAGER_LINKING: NO
packages:
  AinkradAppKit:
    url: https://github.com/AhmedMElhalaby/AinkradAppKit
    # EXACTLY the revision the host's build checks out. Verify, never copy:
    #   git -C ../Ainkrad/build/SourcePackages/checkouts/AinkradAppKit rev-parse HEAD
    revision: 31f89bc9e186354077035316a832237ce6993a09
targets:
  RavenFeature:
    type: library.static
    platform: macOS
    sources: [Sources/RavenFeature]
    dependencies:
      - package: AinkradAppKit
        embed: false
  RavenPlugin:
    type: bundle
    platform: macOS
    sources: [Sources/RavenPlugin]
    postBuildScripts:
      - script: '"$SRCROOT/scripts/stamp-api-version.sh"'
        name: Stamp AinkradAPIVersion
        inputFiles:
          - $(TARGET_BUILD_DIR)/$(INFOPLIST_PATH)
        basedOnDependencyAnalysis: false
    dependencies:
      - target: RavenFeature
      - package: AinkradAppKit
        embed: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ainkrad.plugin.raven
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Sources/RavenPlugin/Info.plist
        MACH_O_TYPE: mh_bundle
        LD_RUNPATH_SEARCH_PATHS: "@executable_path/../Frameworks"
        OTHER_LDFLAGS: ["-lRavenFeature"]
  RavenFeatureTests:
    type: bundle.unit-test
    platform: macOS
    sources: [Tests/RavenFeatureTests]
    dependencies:
      - target: RavenFeature
      - package: AinkradAppKit
schemes:
  RavenPlugin:
    build: { targets: { RavenPlugin: all, RavenFeatureTests: [test] } }
    test:  { config: Debug, targets: [RavenFeatureTests] }
